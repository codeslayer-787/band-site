function askInstrument(){
    // Added functions on line 1 and 5
    let instrument = prompt('What Instrument Will You Play?');
    document.write(instrument);
    return instrument;
    // Added line 8 invocation 
}
let instrument = askInstrument();

